#' @param simulations A [shiny::reactive()] object returning the output of
#'   [fun.bayes.jags()], a standard Expostats function. The latter generates
#'   simulated values of the mean and standard deviation (on the log scale).
